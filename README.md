# 📘 **Sales Analysis using SQL + Python**

This project shows how to connect Python with a SQLite database, run simple SQL queries, load results into Pandas, and visualize sales performance with a basic bar chart.

---

## 🔧 **Tools Used**

* **Python**
* **SQLite (via sqlite3)**
* **Pandas**
* **Matplotlib**

---

## 📂 **What the Script Does**

1. Creates a small SQLite database: **`sales_data.db`**
2. Creates a table: **`sales`**
3. Inserts sample sales records
4. Runs SQL queries to calculate:

   * Total quantity sold per product
   * Total revenue per product
5. Displays results using `print()`
6. Plots a simple bar chart of revenue by product

---

## 🎯 **Skills Practiced**

* Writing SQL queries
* Working with SQLite in Python
* Importing SQL results into Pandas
* Creating simple data visualizations
